console.log('Chức năng của callback function 1.Xử lý bất đồng bộ: Trong JavaScript, callback function thường được sử dụng để xử lý các tác vụ bất đồng bộ như đọc file, gửi yêu cầu HTTP, hoặc xử lý các tác vụ đòi hỏi thời gian xử lý dài mà không chặn luồng thực thi của chương trình và thứ 2 là Xử lý sự kiện (event handling) Callback function thường được sử dụng để xử lý các sự kiện như khi người dùng click vào một nút, submit form, hoặc khi một tác vụ hoàn thành trong một thời gian nhất định. Cách callback function hoạt động trong JavaScript là Trong JavaScript, hàm được coi là một loại dữ liệu đầu tiên (first-class citizen), có nghĩa là chúng có thể được truyền như các tham số vào các hàm khác, được gán cho biến và được trả về từ một hàm khác. Khi bạn truyền một hàm vào một hàm khác làm đối số, hàm đó được gọi là callback function. Hàm cha có thể gọi lại callback function này sau một thời gian hoặc khi một điều kiện nào đó được thỏa mãn.')